import { namespace } from 'vuex-class'

export const api = namespace('api')
export const app = namespace('app')
export const auth = namespace('auth')
export const project = namespace('project')
export const projects = namespace('projects')
export const sharedDimension = namespace('sharedDimension')
export const sharedDimensions = namespace('sharedDimensions')
